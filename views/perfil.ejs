<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Perfil - Administrador</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container mt-5" style="max-width: 500px;">
    <h3 class="mb-4 text-center">🔐 Administrador de Perfil</h3>

    <% if (mensaje) { %>
      <div class="alert <%= mensaje.includes('✅') ? 'alert-success' : 'alert-danger' %> text-center">
        <%= mensaje %>
      </div>
    <% } %>

    <form action="/perfil" method="POST">
      <div class="mb-3">
        <label>Contraseña actual:</label>
        <input type="password" name="claveActual" class="form-control" required>
      </div>
      <div class="mb-3">
        <label>Nueva contraseña:</label>
        <input type="password" name="nuevaClave" class="form-control" required>
      </div>
      <button type="submit" class="btn btn-primary w-100">Cambiar Contraseña</button>
      <a href="/" class="btn btn-secondary mt-3 w-100">← Volver al panel</a>
    </form>
  </div>

</body>
</html>
