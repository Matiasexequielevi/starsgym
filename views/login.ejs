<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - STARS GYM</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(to bottom right, #00eaff, #a8f8ff);
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      position: relative;
      overflow: hidden;
      font-family: 'Segoe UI', sans-serif;
    }

    body::before {
      content: "";
      position: absolute;
      top: 50%;
      left: 50%;
      width: 500px;
      height: 500px;
      background: url('/images/logo-stars.png') no-repeat center center;
      background-size: contain;
      opacity: 0.04;
      transform: translate(-50%, -50%);
      z-index: 0;
    }

    .login-container {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      position: relative;
      z-index: 1;
    }

    .login-logo {
      width: 140px;
      margin-bottom: 1.5rem;
      animation: girarExpandir 1.8s ease-in-out forwards;
      opacity: 0;
      transition: all 0.5s ease;
    }

    .login-logo:hover {
      transform: rotateY(360deg) scale(1.2);
      filter: drop-shadow(0 0 10px #00eaff);
    }

    @keyframes girarExpandir {
      0% {
        transform: rotateY(0deg) scale(0.5);
        opacity: 0;
        filter: brightness(0.5);
      }
      50% {
        transform: rotateY(180deg) scale(1.2);
        opacity: 0.6;
        filter: brightness(1.5);
      }
      100% {
        transform: rotateY(360deg) scale(1);
        opacity: 1;
        filter: brightness(1);
      }
    }

    .login-box {
      background: white;
      padding: 2rem;
      border-radius: 16px;
      border: 2px solid black;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
      width: 100%;
      max-width: 400px;
    }

    .login-box .form-label {
      font-weight: 500;
      color: #333;
    }

    .btn-primary {
      background-color: #00b7c2;
      border-color: #00b7c2;
      font-weight: bold;
    }

    .btn-primary:hover {
      background-color: #0097a5;
      border-color: #0097a5;
    }

    footer {
      text-align: center;
      padding: 1rem;
      background-color: #fdfdfd;
      border-top: 2px solid black;
      color: #333;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>

  <div class="login-container">
    <img src="/images/logo-stars.png" alt="Logo STARS" class="login-logo">
    <div class="login-box">
      <form action="/reportes/login" method="POST">
        <div class="mb-3">
          <label for="clave" class="form-label">Contraseña de acceso</label>
          <input type="password" class="form-control" id="clave" name="clave" required>
        </div>
        <div class="d-grid">
          <button type="submit" class="btn btn-primary">Ingresar a Reportes</button>
        </div>
      </form>
    </div>
  </div>

  <footer>
    Todos los derechos reservados - Levi Prog.
  </footer>

</body>
</html>
