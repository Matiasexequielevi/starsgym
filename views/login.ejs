<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Inicio de Sesión - STARS GYM</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    body {
      background: linear-gradient(to bottom right, #b8f1ff, #d6fcff);
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      position: relative;
      overflow: hidden;
      font-family: 'Segoe UI', sans-serif;
    }

    body::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 200%;
      height: 200%;
      background-image: url('/images/logo-stars.png');
      background-repeat: repeat;
      background-size: 150px;
      opacity: 0.025;
      z-index: 0;
      transform: rotate(-15deg);
    }

    .login-container {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      position: relative;
      z-index: 1;
    }

    .logo-header {
      background-color: white;
      border: 2px solid black;
      border-bottom: none;
      padding: 1rem 2rem;
      border-radius: 0 0 16px 16px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.12);
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .login-logo {
      width: 120px;
      height: 120px;
      object-fit: cover;
      border-radius: 50%;
      animation: aparecer 2s ease forwards;
      filter: drop-shadow(0 0 12px #00eaffaa);
      transition: transform 0.5s, filter 0.5s;
    }

    .login-logo:hover {
      transform: scale(1.15) rotateY(360deg);
      filter: drop-shadow(0 0 18px #00eaff);
    }

    @keyframes aparecer {
      0% {
        transform: scale(0.5);
        opacity: 0;
        filter: brightness(0.4);
      }
      100% {
        transform: scale(1);
        opacity: 1;
        filter: brightness(1);
      }
    }

    .login-box {
      background: white;
      padding: 2rem;
      border-radius: 16px;
      border: 2px solid black;
      border-top: none;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
      width: 100%;
      max-width: 400px;
    }

    .form-label {
      font-weight: 500;
      color: #333;
    }

    .btn-primary {
      background-color: #00b7c2;
      border-color: #00b7c2;
      font-weight: bold;
    }

    .btn-primary:hover {
      background-color: #0097a5;
      border-color: #0097a5;
    }

    footer {
      text-align: center;
      padding: 1rem;
      background-color: #fdfdfd;
      border-top: 2px solid black;
      color: #333;
      font-size: 0.9rem;
    }
  </style>
</head>

<body>
  <div class="login-container">
    <div class="logo-header">
      <img src="/images/logo-stars.png" alt="Logo STARS GYM" class="login-logo" />
    </div>

   <div class="login-box"> 
  <form action="/login" method="POST">
    
    <div class="mb-3">
      <label for="usuario" class="form-label">Usuario</label>
      <input type="text" class="form-control" id="usuario" name="usuario" required />
    </div>

    <div class="mb-3">
      <label for="clave" class="form-label">Contraseña</label>
      <input type="password" class="form-control" id="clave" name="clave" required />
    </div>

    <div class="d-grid">
      <button type="submit" class="btn btn-primary">Ingresar al sistema</button>
    </div>

  </form>
</div>


  <footer>
    Todos los derechos reservados - M.L DESARROLLOS WEB.
  </footer>
</body>
</html>
