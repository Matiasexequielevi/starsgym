<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Clave de acceso - Reportes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="d-flex justify-content-center align-items-center vh-100 bg-light">
  <div class="card p-4 shadow" style="max-width: 400px; width: 100%;">
    <h4 class="mb-3 text-center">Clave para acceder a reportes</h4>
    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    <form action="/reporte-verificar" method="POST">
      <div class="mb-3">
        <label for="claveSecreta" class="form-label">Clave</label>
        <input type="password" class="form-control" name="claveSecreta" required />
      </div>
      <div class="d-grid">
        <button type="submit" class="btn btn-dark">Acceder</button>
      </div>
    </form>
  </div>
</body>
</html>

